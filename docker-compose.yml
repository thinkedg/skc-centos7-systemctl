---

version: '3'
services:

  skcsystemctl:
    build: skc-systemctl

    image: skc-centos7-systmectl

    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
